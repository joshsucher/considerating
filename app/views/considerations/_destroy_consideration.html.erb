
	<% if current_user && current_user.admin? %>
    	<%= link_to "delete", @consideration, :method => :delete,
    									  		:remote => true,
    									  		:class => "delete_consideration",
                                     	  		:confirm => "You sure?" %> • 
	<% end %>
